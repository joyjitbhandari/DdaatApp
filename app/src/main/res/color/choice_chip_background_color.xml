<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled -->
    <item android:color="@color/black_bg" android:state_enabled="false"/>

    <!-- Selected -->
    <item android:color="@color/blue_bg" android:state_selected="true"/>
    <item android:color="@color/blue_bg" android:state_checked="true"/>

    <!-- Not selected -->
    <item android:color="@color/black_bg" />
</selector>